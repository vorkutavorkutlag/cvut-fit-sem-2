
When assigning a child class instance to an ancestor, the type gets decayed, and in the variable only the ancestor part gets saved. This slicing.
Polymorphism is changing methods that exist in the parent class via overriding in a child class. The idea of having many forms. Dealing with the hierarchy of classes.
Achieve dynamic binding using virtual functions.

References, pointers are usually of the base class.
Downcasting, Upcasting...
Virtual class (abstract).

Can do downcast via dynamic cast.

No reflect in C++, dynamically checking methods and attributes...

This was a weird and disappointing class. Why am I paying for this?

